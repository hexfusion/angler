Database  cat_sub  cat_sub.txt      __SQLDSN__
Database  cat_sub  DEFAULT_TYPE text
Database  cat_sub  COLUMN_DEF   "code=char(20) NOT NULL PRIMARY KEY"
Database  cat_sub  COLUMN_DEF   "sel=CHAR(64) DEFAULT '' NOT NULL, index(sel)"
Database  cat_sub  COLUMN_DEF   "name=CHAR(64) DEFAULT '' NOT NULL, index(name)"
Database  cat_sub  COLUMN_DEF   "sort=CHAR(4) DEFAULT 'ZZ' NOT NULL, index(sort)"
